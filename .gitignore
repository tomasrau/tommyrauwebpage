# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# brand catalog & design system source (internal, not needed at runtime)
src/assets/media/design_system/

# material de origen del webinar (local, a propósito)
# El documento maestro define el diseño pedagógico y el posicionamiento
# comercial del curso. Astro lo ignora —no es una content collection, no
# aparece en el sitio— y este repo es PÚBLICO, así que commitearlo lo
# dejaría legible para cualquiera de forma permanente. Se mantiene local.
src/content/learning/
